Add your real login illustrations, logos, and icons here.

Suggested usage:
- Keep screen assets inside `src/assets/images/`
- Import them inside `src/components/` or `src/screens/`
- Replace the placeholder badge in `src/components/auth/LoginHero.tsx` or `src/components/auth/LogoBadge.tsx`
